import {Style} from "../../types/styleTypes";

const FormMediaStyle: Style = {
    formWrapperMedia: {
        width: '100%',
        height: '100%',
        borderRadius: 0,
    },

    formInputsGroupMedia: {
        flexDirection: 'column',
    },
}

export default {
    ...FormMediaStyle,
}
